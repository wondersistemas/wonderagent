<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>br.com.wonder</groupId>
    <artifactId>wonderagent-root</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>agent-cli</artifactId>
  <name>WonderAgent :: CLI</name>
  <description>Entry point: modo serviço Windows + comandos Picocli. Produz o .exe via GraalVM Native Image.</description>

  <dependencies>
    <dependency>
      <groupId>br.com.wonder</groupId>
      <artifactId>agent-model</artifactId>
    </dependency>
    <dependency>
      <groupId>br.com.wonder</groupId>
      <artifactId>agent-core</artifactId>
    </dependency>
    <dependency>
      <groupId>br.com.wonder</groupId>
      <artifactId>agent-drivers</artifactId>
    </dependency>
    <dependency>
      <groupId>br.com.wonder</groupId>
      <artifactId>agent-central-client</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-picocli</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-arc</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-oracle</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-agroal</artifactId>
    </dependency>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>io.quarkus.platform</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>generate-code</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>native</id>
      <properties>
        <quarkus.native.enabled>true</quarkus.native.enabled>
        <!-- x86-64-v2: requer apenas SSE4.2 (Sandy Bridge 2011+), compatível com servidores de clientes antigos -->
        <!-- Sem esta flag o padrão do GraalVM é x86-64-v3 (AVX2, Haswell+), que não roda em hardware mais velho -->
        <quarkus.native.additional-build-args>-march=compatibility</quarkus.native.additional-build-args>
        <!-- Flags de reflection/init centralizadas em META-INF/native-image/br.com.wonder/wonderagent/ -->
        <!-- Banco de dev para smoke test do Native Image — sobrescreva com -Ddb.url=... -->
        <db.url>jdbc:oracle:thin:@//192.168.0.74:1521/DESENV</db.url>
        <db.username>wonder</db.username>
        <db.password>wonder</db.password>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>3.3.0</version>
            <executions>
              <execution>
                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <environmentVariables>
                <DB_URL>${db.url}</DB_URL>
                <DB_USERNAME>${db.username}</DB_USERNAME>
                <DB_PASSWORD>${db.password}</DB_PASSWORD>
              </environmentVariables>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
